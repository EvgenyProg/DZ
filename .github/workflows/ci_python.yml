# Generated by GhostCMD (ci gen)
# ghost-intent: "python + docker + lint + coverage"
# ghost-plan-sha: 3e557fd052390635c1d7f1e252497f893073ed960265ee607f1f60ddd353ae74
name: CI (Generated by GhostCMD)
'on':
  push:
    branches:
    - main
  pull_request: {}
  workflow_dispatch: {}
env:
  ENV: dev
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/${{ github.repository }}:${{ github.sha }}
  RELEASE_NAME: ghostcmd-app
jobs:
  docker_build:
    name: docker_build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Docker login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_TOKEN }}
      - name: Docker build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          file: Dockerfile
          tags: |
            ghcr.io/${{ github.repository }}:sha-${{ github.sha }}